<div class="hero mb-5" style="background-image: url('<%= asset_path("image.jpg") %>')">
  <div class="hero-overlay"></div>

  <div class="hero-content container-fluid">
    <h1 class="hero-title">
      コーデを、もっと気軽に。
    </h1>

    <p class="hero-text">
      Stylogは、写真1枚＋ひとこと、で投稿できるコーデ記録アプリです。
      気負わず、日常のスタイルを残せます。
    </p>

    <div class="d-flex align-items-center gap-3 mt-4">
      <%= link_to "はじめる", new_user_registration_path, class: "btn btn-primary btn-lg px-4" %>
      <%= link_to "投稿を見る", posts_path, class: "btn btn-outline-light btn-lg" %>
    </div>
  </div>
</div>

<div class="row g-4">
  <div class="col-md-4">
    <div class="card h-100">
      <div class="card-body">
        <h3 class="h5 fw-bold">投稿しやすい</h3>
        <p class="text-muted mb-0">入力は最小限。コーデ日記感覚で続けられます。</p>
      </div>
    </div>
  </div>

  <div class="col-md-4">
    <div class="card h-100">
      <div class="card-body">
        <h3 class="h5 fw-bold">見やすい</h3>
        <p class="text-muted mb-0">画像中心で、他人のリアルなスタイルを参考にできます。</p>
      </div>
    </div>
  </div>

  <div class="col-md-4">
    <div class="card h-100">
      <div class="card-body">
        <h3 class="h5 fw-bold">将来の拡張</h3>
        <p class="text-muted mb-0">好みデータを蓄積し、提案機能につなげられる設計に。</p>
      </div>
    </div>
  </div>
</div>

<section class="mt-5">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h2 class="h5 fw-bold mb-0">最新の投稿</h2>
    <%= link_to "すべて見る", posts_path, class: "text-decoration-none small fw-semibold" %>
  </div>

  <div class="row g-3">
    <% @latest_posts.each do |post| %>
      <div class="col-6 col-md-3">
        <div class="card h-100 shadow-sm">
          <%= link_to post_path(post), class: "text-decoration-none text-dark" do %>
            <div class="insta-thumb-wrap">
              <% if post.image.attached? %>
                <%= image_tag post.image, class: "insta-thumb" %>
              <% else %>
                <div class="insta-thumb-wrap d-flex align-items-center justify-content-center text-muted">
                  No Image
                </div>
              <% end %>
            </div>

            <div class="card-body py-2">
              <div class="fw-semibold small">
                <%= post.user&.username.presence || "名無しユーザー" %>
              </div>
              <div class="text-muted small">
                <%= l post.created_at, format: :short %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</section>